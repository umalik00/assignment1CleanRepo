{
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "jest": "^27.3.1",
    "nodemon": "^2.0.15",
    "supertest": "^6.1.6"
  },
  "scripts": {
    "start": "node app",
    "dev": "nodemon app",
    "test": "jest --watchAll --silent -- watch",
    "coverage": "jest --coverage --silent"
  }
}
